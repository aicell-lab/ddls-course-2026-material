# Copy to .env and fill in the key handed out in the course.
# .env is gitignored — never commit the real key.

# OpenAI API key for the course model `gpt-5.6-luna`.
# Used by the Codex CLI for both the data-owner agent and your data-analyst agent.
OPENAI_API_KEY=sk-proj-...
